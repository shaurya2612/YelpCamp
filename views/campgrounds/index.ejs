<%- include ('../partials/header') %>

<div class="container" style="margin-top:20px;">
    
    <header class="jumbotron">
        <h1>Welcome to YelpCamp.</h1>
         <a href="/campgrounds/new">
        <button class="btn btn-primary">
        Add New Camp Ground!
        </button>
        </a>
    </header>


<div class="row text-center" style="display:flex; flex-wrap:wrap;">
    <% for(var i=0;i<campgrounds.length;i++){ %>
            <div class="col-md-3 col-sm-6">
                <div class="img-thumbnail" style:"text-align:center">
                    <img class="img-fluid" src=<%= campgrounds[i].image %>>
                    <div class="container"><caption><strong><%=campgrounds[i].name%></strong></h4></div>
                    <p>
                      <a href="/campgrounds/<%=campgrounds[i].id %>" class="btn btn-primary">Show Info</a>  
                    </p>
                </div>
            </div>
    <% } %>
</div>

</div>
<%- include ('../partials/footer') %>